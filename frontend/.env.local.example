# Server-side only (used by app/api/simulate-whale/route.ts)
MANTLE_RPC_URL=https://rpc.sepolia.mantle.xyz
WHALE_PRIVATE_KEY=0xyour_burner_whale_private_key
VAULT_ADDRESS=0x...
METH_ADDRESS=0x...

# Shared secret: the agent must send this in the `x-agent-secret` header when
# pushing to /api/status. Leave blank to accept unauthenticated pushes (local).
AGENT_PUSH_SECRET=
